# Prometheus scrape config for the observability profile. The slice
# exposes stdlib-generated exposition on /metrics (internal/httpmw);
# route-labelled counters and duration histograms.
global:
  scrape_interval: 5s

scrape_configs:
  - job_name: ghost-trace
    static_configs:
      - targets: ["ingestion:8080"]
